{
  "name": "esbuild-sideeffects",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "esbuild app.js --bundle --outdir=out"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "esbuild": "^0.12.8"
  }
}
